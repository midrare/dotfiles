[Unit]
Description=Dropbox background daemon
After=user-runtime-dir.target network.target
Wants=network-online.target

[Service]
Type=simple
ExecStartPre=-/bin/pkill -x dropbox
ExecStart=/bin/dropbox
ExecStop=/bin/pkill -x dropbox
Restart=on-failure
RestartSec=1

[Install]
WantedBy=default.target
